export enum EmployeeRole {
  EMPLOYEE = "employee",
  MANAGER = "manager",
  HR = "hr",
  ADMIN = "admin",
}

export enum EmploymentType {
  FULL_TIME = "full-time",
  PART_TIME = "part-time",
  INTERN = "intern",
  CONTRACT = "contract",
}

export enum EmployeeStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  SUSPENDED = "suspended",
}